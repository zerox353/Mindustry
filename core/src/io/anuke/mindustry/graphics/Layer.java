package io.anuke.mindustry.graphics;

public enum Layer{
    /** Base block layer. */
    block,
    /** for placement */
    placement,
    /** Only conveyor items. */
    conveyor,
    /** "High" blocks, like turrets. */
    turret,
    /** Power lasers. */
    power
}
